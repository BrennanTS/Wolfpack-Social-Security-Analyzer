# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results
playwright-report
validation/crosscheck/report-*.json

# Brainstorming session mockups
.superpowers/

# v8 coverage output (npm run coverage). The thresholds live in
# vitest.config.ts; the report itself is a local artifact.
coverage/

# The compliance copy schedule is generated (npm run copy:schedule) and
# stamped with a date, so committing it would churn. Regenerate to send.
validation/copy/copy-schedule.docx
